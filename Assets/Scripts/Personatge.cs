using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Personatge : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //[SerializeField]

        //private GameObject balaPrefab;
        //private GameObject bala;


        //transform.Translate(0, 0, velocidad* Time.deltaTime);

        //Ray rayo = new Ray(transform.position, transform.forward);
        //RaycastHit hit;


        //if (Physics.SphereCast(rayo, 0.75f, out hit))
        //{
        //Nuevo código. Ponemos inmediatamente este despues del if
        //GameObject golpearObjecto = hit.transform.gameObject;
        //if (golpearObjecto.GetComponent<PlayerCaracter>())
        //{
        // if (bala == null)
        //{
        //bala = Instantiate(balaPrefab) as GameObject;
        //bala.transform.position =
        //transform.TransformPoint(Vector3.forward* 1.5f);
        //bala.transform.rotation = transform.rotation;
        //}
        //}//El siguiente if es el que ya teniamos desde el codigo anterior solo ponlo adentro del if
        //else if (hit.distance<rangoObstaculo)
        //{
        //float angulo = Random.Range(-110, 110);
        //transform.Rotate(0, angulo, 0);
        //}
        //}
        //} 

    }
}
